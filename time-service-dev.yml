server:
  port: 0

eureka:
  instance:
    metadataMap:
      instanceId: ${instanceId}
  client:
    registerWithEureka: true
    fetchRegistry: true
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
